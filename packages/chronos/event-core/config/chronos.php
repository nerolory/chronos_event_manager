<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | Цветовой акцент по умолчанию
    |--------------------------------------------------------------------------
    | HEX-цвет для новых событий без явно указанного цвета.
    */
    'default_color' => env('CHRONOS_DEFAULT_COLOR', '#38BDF8'),

    /*
    |--------------------------------------------------------------------------
    | Максимальное количество вхождений при разворачивании RRULE
    |--------------------------------------------------------------------------
    | Защита от бесконечных серий без COUNT/UNTIL.
    */
    'max_occurrences' => env('CHRONOS_MAX_OCCURRENCES', 365),

    /*
    |--------------------------------------------------------------------------
    | Временная зона по умолчанию
    |--------------------------------------------------------------------------
    | Используется если у пользователя не задан timezone в user_settings.
    */
    'default_timezone' => env('CHRONOS_DEFAULT_TIMEZONE', 'UTC'),

];
